[project]
name = "website"
version = "0.1.0"
description = "Acoular organization website"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "acoular-sphinx",
]

[tool.uv.sources]
acoular-sphinx = { git = "https://github.com/acoular/ci.git", branch = "main", subdirectory = "packages/acoular-sphinx" }
